import ProjectCarousel from "@/components/widgets/ProjectCarousel"


export const ProjectList = () => {
  
  return (
    <section id="projects">
      
      <ProjectCarousel />
    </section>
  );
}